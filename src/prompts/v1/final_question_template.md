Триггер сработал для `{{TRIGGERED_PAIR}}`.

**Причина вызова:** `{{TRIGGER_REASON}}`. **Текущий макро-контекст:** `{{MACRO_TEXT}}` (Индекс: `{{MACRO_VALUE}}`).

Проанализируй `{{TRIGGERED_PAIR}}` (включая `market_data` и `technical_analysis`) в контексте всего портфеля (`account_state`).

Действуй в рамках своей роли (из `system prompt`) и `risk_rules` (из `strategy_context`).

**КРИТИЧЕСКИ ВАЖНО:** Перед открытием позиции обязательно проверь, что рассчитанный размер позиции не превышает `account_state.max_position_size_usdt`. Используй формулу из раздела 4 системного промпта для расчета. Если размер позиции превышает баланс, скорректируй `risk_percent` или дистанцию до стопа, либо выбери `HOLD`.

Прими торговое решение на основе предоставленных данных. Помни о необходимости обоснования каждого решения в поле `justification` с использованием цепочки размышлений (Chain-of-Thought), включая проверку баланса.

Сформулируй свой анализ в поле `justification` и верни JSON-ответ, строго соответствующий указанной схеме.

